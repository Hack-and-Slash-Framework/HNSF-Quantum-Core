event PlayVisualEffectAtLocation3D
{
    asset_ref<VisualEffectEntry> visualEffectRef;
    entity_ref parent;
    bool parented;
    nothashed bool positionAsOffset;
    nothashed FPVector3 position;
    nothashed bool rotationAsOffset;
    nothashed FPVector3 rotation;
    nothashed FPVector3 scale;
    bool atClosestBodyPosition;
    FPVector3 sourcePosition;
    bool setRotationToForceDir;
    asset_ref<Tag> parentBoneTag;
}